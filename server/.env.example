PORT=3000

# Option 1: direct connection string
# MONGODB_URI=mongodb://localhost:27017/capture_extension

# Option 2: MongoDB Atlas (cloud)
MONGODB_USER=lavguptabihiya_db_user
MONGODB_PASSWORD=8QrGMnLLH7gm4guZ
MONGODB_CLUSTER=cluster0.example.mongodb.net
MONGODB_DB_NAME=capture_extension

# Comma-separated list of allowed frontend origins.
CORS_ORIGIN=http://localhost:5173,https://guptagpt-frontend.vercel.app
JWT_SECRET=replace-with-long-random-secret
GOOGLE_CLIENT_ID=your-google-oauth-web-client-id.apps.googleusercontent.com

# Optional: force a MongoDB binary version for mongodb-memory-server.
# Useful when the latest binary crashes on your local machine.
# MONGOMS_VERSION=7.0.14
