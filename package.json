{
  "name": "page-capture-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc && cp src/manifest.json dist/manifest.json && mkdir -p dist/popup && cp src/popup/index.html src/popup/styles.css dist/popup/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.276",
    "typescript": "^5.8.3"
  }
}
